#include <stdio.h>
#include <string.h>

int checkInput(char input)	{
	
	int i, j, count, breakPoint;
	count = 1;
	breakPoint = 0;
	
	
	
	for (i = 0; i < strlen(input); ++i)	{
		
		if (0 == strcmp(" ", input[i]))	{
			
			for (j = breakPoint; j < i; ++j)	{
				
				switch (count)	{
						
					case 1 :
						
						
				}
			}
		}
	}
	
	char type[255];
	double start, end;
	int interRows result;
	
	
}
